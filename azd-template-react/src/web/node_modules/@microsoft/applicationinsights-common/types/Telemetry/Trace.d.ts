import { IDiagnosticLogger } from "@microsoft/applicationinsights-core-js";
import { FieldType } from "@microsoft/applicationinsights-common/types/Enums";
import { IMessageData } from "@microsoft/applicationinsights-common/types/Interfaces/Contracts/IMessageData";
import { SeverityLevel } from "@microsoft/applicationinsights-common/types/Interfaces/Contracts/SeverityLevel";
import { ISerializable } from "@microsoft/applicationinsights-common/types/Interfaces/Telemetry/ISerializable";
export declare class Trace implements IMessageData, ISerializable {
    static envelopeType: string;
    static dataType: string;
    aiDataContract: {
        ver: FieldType;
        message: FieldType;
        severityLevel: FieldType;
        properties: FieldType;
    };
    /**
     * Schema version
     */
    ver: number;
    /**
     * Trace message
     */
    message: string;
    /**
     * Trace severity level.
     */
    severityLevel: SeverityLevel;
    /**
     * Collection of custom properties.
     */
    properties: any;
    /**
     * Collection of custom measurements.
     */
    measurements: any;
    /**
     * Constructs a new instance of the TraceTelemetry object
     */
    constructor(logger: IDiagnosticLogger, message: string, severityLevel?: SeverityLevel, properties?: any, measurements?: {
        [key: string]: number;
    });
}
